# Backlog — pure UI/UX loop (shadcn migration)

One item per iteration, top first. Check off when done + add a line to
`progress-current.md`. Drop an item (with a one-line reason) if it's not worth
doing. Keep it alive — append concrete items when it runs thin.

Goal: every view on shadcn/ui, no hand-rolled component where shadcn has one,
behavior identical, tests green. See `PROMPT.md` + `UI-RULES.md`.

Format: `- [ ] (band) item — acceptance`

## A · Scaffold (must land first) — DONE (set up manually; installs need network the loop's sandbox lacks)
- [x] (A) Initialise shadcn in `web/` (radix base, Nova preset, neutral base color, Tailwind v4 via `@tailwindcss/vite`, `components.json`, tokens in `src/styles.css`, `cn()` in `src/lib/utils.ts`). Fixed the alias bug (CLI wrote a literal `@/` dir → relocated into `src/`, added `paths` to root `tsconfig.json`). Scaffold smoke test proves the `@/` alias resolves.
- [x] (A) Added the core component set to `src/components/ui/` (24): button card badge alert dialog alert-dialog input select switch checkbox label table tabs separator skeleton sonner tooltip sidebar sheet field empty spinner dropdown-menu avatar + `use-mobile` hook.

## B · App shell
- [x] (B) Rebuild `Layout.tsx` as a shadcn `Sidebar` shell — DONE: SidebarProvider/Sidebar/SidebarInset, lucide icons, `data-active` highlight, top-bar page label, footer sign-out (collapses to a Sheet on mobile via SidebarTrigger). Deleted dead Layout-only CSS; added `window.matchMedia` stub to test/setup.ts. 93 web tests green.

## C · Shared primitives (replace hand-rolled)
- [x] (C) Badge family (`Badge`/`ReadOnlyBadge`/`ResultBadge`) → shadcn `Badge` — DONE: extended `badge.tsx` cva with `success`/`warning`/`info` variants (backed by new `--color-*`/`-soft` theme tokens aliasing legacy `--ok/--warn/--info`); ui.tsx wrappers now map tone→variant over `<ShadcnBadge>`; deleted dead `.badge*` CSS; `ui.test.tsx` ResultBadge asserts `data-variant`. Public API unchanged, callsites untouched. 93 tests green.
- [x] (C) Alert family (`Callout`/`ErrorNotice`/`StaleBanner`) → shadcn `Alert` — DONE: extended `alert.tsx` cva with soft-tinted `success`/`warning`/`info` variants (+ destructive soft bg + `border-l-4 border-l-<tone>` accent) backed by the shared `--color-*` tokens; added `data-variant` to `<Alert>`. `Callout`/`ErrorNotice`/`StaleBanner` recomposed over `Alert`+`AlertTitle`/`AlertDescription`, same public API, role="alert" + labelForCode + hint preserved; 40+ callsites untouched. Also migrated two raw callout divs (`ConfirmDialog`, `Login`) to the shared `Callout`. Deleted dead `.callout` container/color CSS (kept `.callout-detail`/`.callout-hint` content classes; scoped hint color inside Alert). Tests migrated off `.callout-*` onto `data-variant` (Backups ×9, ConfirmDialog, ui StaleBanner). 93 tests green.
- [x] (C) `Spinner` → shadcn `Spinner` — DONE: recomposed the `ui.tsx` `Spinner({label})` wrapper over the shadcn `Spinner` primitive (`Loader2Icon`+`animate-spin`), keeping its public API + muted status row (now Tailwind semantic tokens). All 14 callsites untouched. Deleted dead `.loading`/`.spinner`/`@keyframes spin` CSS. A11y: row is an explicit `role="status"` live region (`aria-live=polite`/`aria-atomic`), inner icon decorative (`role=presentation`+`aria-hidden`) to avoid nested status. Added `Spinner` tests. 95 web tests green.
- [x] (C) `Modal.tsx` → shadcn `Dialog` — DONE: recomposed the hand-rolled `Modal({open,title,onClose,children,footer,tone,width})` over shadcn `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogFooter`, keeping the exact public API so all ~10 callsites (Alerts, Migrate, Backups, RolesDatabases, ConfirmDialog) stay untouched. Radix now owns the focus trap / Escape / backdrop dismiss / focus restore the old version hand-rolled. width→`sm:max-w-md|xl|3xl`; `tone="danger"`→`ring-destructive/30` + `data-tone`; busy-noop `onClose` keeps it open via controlled `open` overriding `onOpenChange`. Body scrolls in an inner `max-h-[65vh] overflow-y-auto` wrapper so the pinned close button isn't clipped (reviewer finding); `aria-describedby={undefined}` to opt out of Radix's dangling description ref (reviewer finding). Deleted dead `.modal*` CSS + orphaned `@keyframes fade/pop` (kept `.confirm-message`). 95 tests green. NOTE: Modal.tsx remains as a thin Dialog shell (same pattern as Layout→Sidebar, Badge/Alert/Spinner wrappers) — full per-callsite decomposition + `.btn`→`Button` is deferred to band D/E when each view is migrated.
- [x] (C) `ConfirmDialog.tsx` → shadcn `AlertDialog` — DONE: rebuilt `ConfirmDialog` + `TypedConfirmDialog` on `AlertDialog` (no click-outside dismiss, focus trap, forced choice). Controlled `open` (parent owns close); action/cancel handlers `preventDefault()` to stop Radix auto-close; Escape ignored while busy. Buttons → `AlertDialogAction`/`AlertDialogCancel`; danger→`variant="destructive"`. Typed-name input → shadcn `Field`+`FieldLabel`+`Input`, gate intact. Message via `AlertDialogDescription asChild`/`<div>` so callsite block content stays valid. `aria-busy` added. Public API unchanged, 6 callsites untouched; test migrated to `data-variant`; dead `.confirm-message` CSS removed. 95 tests green.
- [x] (C) `Toast.tsx` → `sonner` — DONE: deleted `Toast.tsx` (ToastProvider/useToast); mounted `<Toaster/>` (`ui/sonner.tsx`) once at App root in `App.tsx`. Call sites already called `toast.success/error/info(msg)` = sonner's API, so the ~15 callers (7 files) just swapped `useToast()` for `import { toast } from "sonner"` — firing calls unchanged. Simplified generated `sonner.tsx`: dropped `next-themes` (no ThemeProvider in this SPA; `theme="system"` follows the `prefers-color-scheme` @media the app already uses) + added `closeButton` (× parity). Deleted dead `.toast*` CSS + `@keyframes slide`. Tests dropped `ToastProvider` wrappers (Layout/Pooler) / swapped the Toast mock for a sonner mock (Migrate). 95 tests green. NOTE: `next-themes` is now an orphan dep — prune in band E.

## D · Views (one per iteration, behavior identical)
- [x] (D) Login → shadcn (Card + Field/FieldGroup + Input + Button) — DONE: centered `<form>` wrapping a `Card` (Header brand+title+desc / Content form / Footer help); body `FieldGroup`+`Field`+`FieldLabel`+`Input`; submit `Button` (`w-full`) composing `Spinner`+"Signing in…"; error via shared `Callout`. Behavior identical (login call, `from` redirect, error copy, clear-on-fail, disabled gating). A11y: Callout `id` + Input `aria-invalid`/`aria-describedby` + Field `data-invalid` (Callout now forwards `...props`). Dead `.login-*`/`.brand-mark`/`.btn-block` CSS removed. Added `Login.test.tsx` (4 tests). 99 web tests green.
- [x] (D) Dashboard → shadcn — DONE: rebuilt on shadcn `Card` composition (Postgres/Latest-backup/Server panels), local `StatCard` over `Card size="sm"` with semantic threshold tinting (`StatCard` deleted from `ui.tsx` — Dashboard was its only user). Hand-rolled layout classes → Tailwind tokens (`card-grid`/`stat-grid`/`kv-list`/`kv`→`grid`/`<dl>`; `bullet-list`→`list-disc`; `updated-at`/`muted`→`text-muted-foreground`). First-load spinner → `Skeleton` (`role=status`+`aria-label`). Behavior identical (poll cadence, 3 render states, `StaleBanner`, no-backup warn `Callout`). A11y: `<dl aria-label>`, `<time dateTime>`. Dead `.card-grid`/`.stat-*`/`.kv-*`/`.updated-at` CSS removed (`.bullet-list` kept for RolesDatabases). +2 tests → 101 green.
- [x] (D) RolesDatabases → shadcn — DONE: all `.btn*`→`Button` (primary CTA=default; card/table actions=`outline sm`; Delete=`destructive sm`; modal Cancel=`outline`; busy states compose the raw `Spinner` via `data-icon`). Both `.data-table`→`Table` family (`.table-scroll` dropped; `.col-actions`→`text-right`+`flex justify-end gap-2`). Forms→`Field`/`FieldLabel`/`FieldDescription`/`FieldError`+`Input`; native `<select>`s→shadcn `Select` (disabled when empty). `NameField` rebuilt on `Field`/`Input` with `data-invalid`+`aria-invalid`+`aria-describedby` (help + FieldError ids); Select triggers `aria-describedby` their hint. `bullet-list`/`muted`/`secrets-list`→Tailwind tokens. Behavior identical (Modal create flows, TypedConfirmDialog typed-name drop, rotate/one-time-secrets, toasts). Shared `Card`/`PageHeader`/`EmptyState`/`Spinner`/`ErrorNotice`/`SecretValue`/`Callout` kept. Deleted dead `.bullet-list`/`.field-error`/`.secrets-list` CSS. Added `RolesDatabases.test.tsx` (5 tests). 106 web tests green. ui-heuristics-reviewer: accepted all 3 a11y findings.
- [x] (D) Query → shadcn — DONE: `<textarea>`→shadcn `Textarea` (added via `shadcn add textarea`); sample `.chip`s→`Button outline sm` (+`title` disclosing they replace the editor); Run→`Button` composing the raw `Spinner` (`data-icon`)+"Running…" busy (dropped the redundant standalone results spinner); results `.data-table`→`Table` family with `.row-num`/`.cell-null`/`.col-name`/`.col-type`→Tailwind tokens. Editor chrome (`.editor-wrap`/`.editor-actions`) → flex/`gap-*`. A11y: editor aria-label "Read-only SQL editor"→"SQL query editor" (field is editable; read-only is on the badge+callout). Behavior identical (Cmd/Ctrl+Enter, read-first guard, auto-LIMIT badge+note, zero-row/zero-col messaging, ErrorNotice). Deleted dead Query-only CSS (`.chip`/`.query-*`/`.editor-*`/`.sql-editor`/`.kbd-hint`/`.results*`); kept shared `.data-table`/`.table-scroll`/`.row-num` etc (Alerts/Backups/Migrate still use them). Added `Query.test.tsx` (5 tests). 111 web tests green. ui-heuristics-reviewer: accepted 3 findings (#1 double-spinner, #2 sample disclosure, #3 misleading aria-label), declined #4 (keep zero-row table headers — they show result shape, parity).
- [x] (D) Backups → shadcn — DONE: PageHeader actions `.btn*`/`.split-btn` → `Button`s (Restore…=`destructive`; Test/Deep/Full=`outline`; Back up now=default; busy composes `Spinner`). Both history tables `.data-table`/`.table-scroll` → `Table` family; per-row error → `CellError` (`truncate`+`text-destructive`); `.compression`/`.mono small` → Tailwind tokens. RestoreModal: radios → `RadioGroup`/`RadioGroupItem` (added via `shadcn add radio-group`); delta checkbox → shadcn `Checkbox`; datetime + typed-confirm → `Input`; `<fieldset>`/`<label>` → `FieldSet`/`FieldLegend`/`Field`/`FieldContent`/`FieldLabel`/`FieldDescription`; footer → `Button`. Typed-stanza confirm gate unchanged. Status banners already shadcn (Callout/Badge/AlertDialog) — left intact. ui-heuristics-reviewer: accepted #1+#2 (added `FieldError` on mismatch + `aria-describedby`) and #3 ("Back up now" title); declined #4 (spinner aria-hidden — matched accepted bare-InlineSpinner pattern), #5 (datetime `max` — behavior change), #6 (CellError tabIndex — not a regression). Exported `RestoreModal`, added 3 tests; added a `ResizeObserver` stub to test/setup.ts (Radix RadioGroup needs it). Dead CSS removed (`.split-btn`/`.split-extra`/`.cell-error`/`.compression`; `.radio` dropped from shared `.radio,.checkbox`). 114 web tests green (+3); typecheck/build/go build green.
- [x] (D) Migrate → shadcn — DONE: mode selector + receive/send → shadcn `Tabs` (first Tabs use; `TabsContent` keeps the unmount-on-switch parity of the old `{mode===...}` render). `SourceFields` `<fieldset>` → `FieldSet`/`FieldLegend`/`FieldGroup`/`Field`/`FieldLabel`/`FieldDescription`+`Input` (two-input rows → responsive `grid`). All forms → `Field`/`Checkbox`; overwrite checkboxes → `Field orientation=horizontal`+`Checkbox`; typed-confirm (target name / `OVERWRITE`) → `Field`+`Input`+`FieldError` on mismatch (gate unchanged). `.btn*` → `Button` (submit self-start; Start-another=`outline sm`; session Cancel=`ghost sm`+`text-destructive`; modal footers outline/destructive; busy composes `Spinner`). History + verification-diff `.data-table` → `Table`. `.session-code*` → `bg-muted`/`font-mono text-4xl`; progress stepper (no shadcn equivalent) kept but rebuilt on semantic tokens via `cn()`. Behavior identical (3 modes, direct + cross-panel, overwrite gates, poll/stale ladder, S3-or-error, verification). ui-heuristics-reviewer: findings all pre-existing → declined behavior/copy changes per parity; applied 2 additive a11y wins (mismatch-table `aria-label`, sr-only `aria-live` stepper status). Added a mode-tabs test (+2 → 116 green). Dead Migrate-only CSS removed. typecheck/build/go build green.
- [x] (D) Alerts → shadcn — DONE: channel cards → shadcn `Card` (Header/Title/Description/Action badge + Footer Edit/Send-test `Button`s); rules `.data-table` → `Table`; Enable/Disable button → `Switch` (aria-label, unchanged `toggleRule`); Delete `.btn-danger-ghost` → `ghost` + `text-destructive`; `.row-disabled` → `cn(!enabled && "opacity-60")`. ChannelModal + RuleModal forms → `Field`/`FieldLabel`/`Input`/`FieldDescription`; native selects (metric/op/severity) → shadcn `Select` (+`aria-labelledby` per reviewer); Enabled checkbox → `Field orientation=horizontal`+`Checkbox`; `.btn*` → `Button`; field rows → responsive `grid`. Severity/State badges already on shared `Badge`. Behavior identical (channel save/test, rule CRUD, toggle, typed-name delete, toasts, loading/empty/error). Added `Alerts.test.tsx` (5 tests). Dead Alerts-only CSS removed (`.channel-grid`/`.channel-card`/`.channel-head`/`.btn-danger-ghost`/`.row-disabled`). 121 web tests green; typecheck/build/go build green.
- [x] (D) Settings → shadcn — DONE: rebuilt the `BackupSettingsForm` S3 form (`Settings.tsx`; `DatabaseTuning`/`Pooler` are separate items). `.field-grid` → `grid gap-5 sm:grid-cols-2`; every `<label className="field">` (span + raw `<input>` + `.field-help`) → `Field`/`FieldLabel`/`Input`/`FieldDescription`; SSL `.checkbox` → `Field orientation=horizontal`+`Checkbox`+`FieldContent`; submit `.btn-row`/`.btn` → `Button` composing `InlineSpinner` on busy. (No Tabs — the form is two Cards, not multi-section; KISS.) Cards/Callouts/Badge already shadcn. Behavior identical (write-only secret/cipher, `Save`↔`Save & connect`, warning/configured callouts, toasts). ui-heuristics-reviewer: applied both blocking findings (additive `aria-describedby` wiring Input/Checkbox→FieldDescription ids — old `<label>`-wrap gave it free); declined pre-existing #1/#4/#5. Added `Settings.test.tsx` (4 tests). Dead `.field-grid` CSS + media query removed; kept shared `.field*`/`.checkbox`/`.btn-row` (Pooler/DatabaseTuning, band E). 125 web tests green; typecheck/build/go build green.
- [x] (D) Pooler → shadcn — DONE: `PoolSettingsTable`'s `<table className="tuning-table">` → shadcn `Table` family (`TableHead scope="row"` row-labels + muted help span, `TableCell` `font-mono tabular-nums` values, `TableCaption`); the `tuning-*` class names had **no** CSS backing so this is a pure styling win. Role-selection `<fieldset className="field">`/`<legend>` → `FieldSet`/`FieldLegend`; `.field-help` → `FieldDescription`; each `<label className="checkbox"><input>` → `Field orientation="horizontal"` + shadcn `Checkbox` (`onCheckedChange`→unchanged `toggle`) + id-paired `FieldLabel`. Enable/Disable `.btn*` → `Button` (default / `destructive`); `.btn-row` → `flex`; "Off" `card-head` → `flex justify-end` + `Badge`; `.muted` → `text-muted-foreground`. Confirm-then-act `ConfirmDialog` flow + effect copy + `!status.pool||selected.length===0` gating untouched (the enable side-effect needs explicit confirmation — a Switch would mis-model it, so the backlog's speculative "Switch toggle" note was dropped). No dead CSS to remove (only the unstyled `tuning-*` were Pooler-specific; shared `.field*`/`.checkbox`/`.btn-row` stay for DatabaseTuning). ui-heuristics-reviewer: declined all 3 (bare-div violates the form rule + shadcn `role=group` is by-design; "caption below" premise false — no `caption-bottom`; describedby on a non-focusable disabled button). Pooler.test.tsx's 12 tests already matched the migrated markup — unchanged, green. 125 web tests; typecheck/build/go build green.
- [x] (D) DatabaseTuning → shadcn — DONE: workload-profile native `<select>` → shadcn `ToggleGroup` (`type="single"`, `variant="outline"`; UI-RULES says a 2–7-option set is a ToggleGroup, not a dropdown — added `toggle-group`/`toggle` via the CLI, stripped the no-op `"use client"`); active item suffixed " — current"; `onValueChange` guards the deselect-empty-string so a profile is always picked (parity). Selector wrapped in `Field` + `FieldTitle` (a `<div>` labelling the ToggleGroup's `role="group"` via `aria-labelledby` — a `<label>` can't target a group) + per-profile effect as `FieldDescription` (`aria-describedby`). `SettingsTable`'s orphaned `<table className="tuning-table">` → shadcn `Table` family (identical to Pooler's `PoolSettingsTable`: font-medium label + muted help, `text-right font-mono tabular-nums` value) — pure styling win, the `tuning-*` names had no CSS backing. `.tuning`/`.tuning-profile` wrappers → `flex flex-col gap-4`. Behavior identical (3 render states, intro/warn/preview callouts, applied + recommendation tables). Test migrated off `combobox`/`fireEvent.change` to `getByRole("radio")`+`fireEvent.click`; 5 green. ui-heuristics-reviewer: applied #1 (`FieldLabel`→`FieldTitle`) + #2 (additive `aria-describedby`); declined #3/#4 (caption-bottom, row-hover) for consistency with the sibling Pooler Table. **Band D complete.** Shared `.field*`/`.checkbox`/`.btn-row` are now unreferenced by any view → band E prune. 125 web tests; typecheck/build/go build green.

## E · Cleanup & consistency
- [x] (E) Shared `ui.tsx` `Card` wrapper → shadcn `Card` — DONE: recomposed `Card({title,actions,children,className})` over shadcn `Card`/`CardHeader`/`CardTitle`/`CardAction`/`CardContent` (band-C wrapper pattern); public API unchanged so all ~8 view call-sites untouched. Divider dropped for parity with Login/Dashboard (shadcn `Card` ring + `gap-(--card-spacing)`). Deleted dead `.card`/`.card-head`/`.card-actions`/`.card-body` CSS. ui-heuristics-reviewer (blocking ×2): `CardTitle` primitive → `<h3>` (preserves the old wrapper heading + restores headings in already-migrated Login/Dashboard/Alerts); restored `<span/>` grid placeholder for actions-only cards. 125 tests green; typecheck/build/go build green.
- [ ] (E) Migrate the LAST hand-rolled `ui.tsx` shared components so the legacy `styles.css` classes go dead. One per iteration — acceptance: grep finds no `className="<legacy>"` outside `styles.css`. Remaining:
  - [x] `EmptyState` → shadcn `Empty` (`.empty`/`.empty-title`/`.empty-hint`) — DONE: recomposed over `Empty`/`EmptyHeader`/`EmptyTitle`/`EmptyDescription`/`EmptyContent`, public API unchanged, 6 call-sites untouched, dead `.empty*` CSS dropped. Reviewer's 4 findings declined as non-regressions (old title was a `<p>` not a heading → no `<h3>`; `border-dashed` has 0 width via preflight → no double border). 125 tests green.
  - [x] `SecretValue` reveal/copy `.btn btn-sm` → shadcn `Button` (`.secret*`) — DONE: Reveal/Hide+Copy → `Button outline sm`; masked `<code>`+`.secret-*` wrappers → Tailwind tokens; public API unchanged, single call-site (RolesDatabases) untouched. Dead `.btn-sm`/`.secret-*` CSS removed. Reviewer's one blocking finding applied (`aria-pressed` on the toggle); copy-error note declined (pre-existing). Added a SecretValue test. 127 tests green.
  - [x] `PageHeader` divs → Tailwind (`.page-header`/`.page-desc`/`.page-actions`) — DONE: row→`flex items-start justify-between gap-4`; desc→`mt-1 max-w-[60ch] text-muted-foreground`; actions→`flex shrink-0 flex-wrap justify-end gap-2`; `h1`→`text-2xl font-semibold text-foreground` (shadcn peer convention). Public API unchanged, 6 call-sites untouched. Dead `.page-*`/`.page-header .btn-row` CSS removed (standalone `.btn-row` kept). Reviewer's one blocking finding applied. Added a PageHeader test. 130 tests green.
  - [x] `App.tsx` `.boot-screen` → Tailwind, and `Settings.tsx` `.callout-detail`/`.callout-hint` → component/Tailwind — DONE: `.boot-screen` → `grid h-screen place-items-center`; `.callout-detail` `<pre>` → `rounded-md border bg-muted ... font-mono text-xs leading-normal` (border per reviewer for a bg-independent edge inside the colored Alert); all `.callout-hint` (Settings + ui.tsx ErrorNotice/StaleBanner, all inside Alerts) → `mt-1.5 text-[13px] opacity-80`. Dead `.callout-*`/`.boot-screen` CSS removed; `ui.test.tsx` migrated off the class querySelector. 130 tests green.
  - [x] `.view` page-wrapper class (the straggler the line above missed) → Tailwind — DONE: all 8 views' root `<div className="view">` → `mx-auto flex max-w-[1100px] flex-col gap-5` (1:1 of the old `.view` rule; gap-5 = 20px). Then deleted ALL now-dead legacy *class* rules from `styles.css` (`.muted`/`.small`/`.mono`, `.view`, `.btn*`/`.icon-btn`, `.data-table*`/`.table-scroll`/`.col-*`/`.row-num`/`.cell-null`, `.field*`/`.checkbox`/`fieldset.field`, dead Modal comment, `.field-row` media query). Kept `:root` token vars + global element styles + `@theme inline` + `.dark` + `@layer base`. Reviewer confirmed exact equivalence, no layout regression. 130 tests green. **Now genuinely zero legacy `className` strings anywhere outside `styles.css`.**
- [x] (E) Delete the hand-rolled `styles.css` design-token *vars* (keep only minimal unavoidable globals) — DONE: migrated the global `body`/`code`/`a` element rules onto shadcn semantic tokens (`body` dropped `font-family:var(--sans)`+`color:var(--text)`+`background:var(--bg)` so the layered `bg-background`/`text-foreground` + `html` `font-sans` Geist take over, keeping only non-token `font-size`/`line-height`/`-webkit-font-smoothing`; `a` → `@apply text-primary`; `code` → `@apply bg-muted font-mono`), then deleted the 5 now-freed vars `--bg`/`--surface-2`/`--text`/`--mono`/`--sans` (grep-proven zero refs outside the edited lines). KEPT `--ok*`/`--warn*`/`--info*` (consumed by `@theme inline` → `--color-success/warning/info`), `--border`/`--primary`, `--radius`. Cascade key: the unlayered `body` rule had been shadowing the layered shadcn baseline. Deliberate visible change (the scaffold's intended baseline, previously shadowed): page bg gray→white, font system-ui→Geist; code/text deltas negligible. Pure semantic-token swap, no markup change → reviewer N/A. 130 tests green; typecheck/build/go build green.
- [ ] (E) Consistency sweep: every empty state uses `Empty`, every loader uses `Skeleton`, every status uses `Badge`, every callout uses `Alert`; no orphaned components remain — acceptance: grep finds no hand-rolled equivalents; then write COMPLETE.md.
- [x] (E) Prune `next-themes` from `package.json` — DONE: `npm uninstall next-themes --offline`
  (no network needed — pure removal) dropped it from `package.json` (`^0.4.6`) + lockfile (−11).
  Confirmed orphan: only a *comment* in `sonner.tsx` mentions it (not imported), and `sonner`
  does not peer-require it. No view/component/markup change → UI-heuristics review N/A. Embedded
  `dist` rebuilt byte-identical (never bundled). 130 web tests green; typecheck/build/go build green.
- [~] (E) DROPPED — Token: `--info-soft` vs `--primary-soft` distinctness. Premise is
  stale: `--primary-soft` no longer exists (removed during the legacy-var prune), so
  `info` badges no longer collide with any primary-soft surface. No distinct-blue change
  needed. (Also: dark `:root` values are gone — the panel is now light-only.)
