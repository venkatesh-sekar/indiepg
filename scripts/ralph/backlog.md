# Backlog

One item per iteration, highest band first. When you finish an item, check it
off and add a dated line to `progress-current.md`. When you drop an item, delete
it with a one-line reason. Keep this list alive — append concrete items when it
runs thin (auditing the panel against the north star is itself a valid iteration).

Format: `- [ ] (band) item — acceptance: how we know it's done`

## 0 · Foundation
- [x] (0) Add vitest + React Testing Library to `web/`; wire `npm test`; add one real test for an existing component — done 2026-06-24: vitest run + RTL/jsdom, config in vite.config.ts, `ui.tsx` component test (6 tests green).
- [x] (0) Add root `AGENTS.md` capturing build/test/run commands + conventions (read-only role, confirm-on-risky, best-defaults) so every iteration is consistent — done 2026-06-24: `AGENTS.md` at repo root references all `make` targets, the web npm scripts/verify gate, and links `scripts/ralph/DEFAULTS.md`; conventions section mirrors README invariants. Reviewed (one doc fix applied).
- [x] (0) Wire the web verify gate into the loop reality: ensure `npm run typecheck && npm run build` pass clean from a fresh `npm ci` — done 2026-06-24: verified the full gate green from a fresh `npm ci` (typecheck/build/test all pass; build is deterministic — committed `internal/server/web/dist` unchanged, tree stays clean). Made the gate executable instead of prose: added `make verify` (fmt-check → vet → test → build), `make verify-web` (npm ci → typecheck → build → test), and `make fmt-check` (the `gofmt -l` must-be-empty check, which `go fmt` does not enforce; fails on unparseable files too). AGENTS.md points at them. Foundation band complete → next is band 1 (security).

## 1 · Security
- [x] (1) Audit session auth: confirm cookies are HttpOnly + SameSite + Secure-where-applicable, session expiry/rotation on login, logout invalidates server-side — done 2026-06-24: cookie hardening (HttpOnly/SameSite=Strict/Secure-aware via r.TLS + opt-in trusted proxy), expiry (fail-closed on zero/expired), and per-login token rotation were already implemented+tested. Closed the missing piece — server-side logout invalidation: logout now rotates the HMAC signing secret (`auth.Logout`→`store.RotateSessionSecret`), killing every issued token, but only when the caller proves a live session (`logoutAuthorized`: valid token + CSRF origin check for cookie flows) so the public endpoint can't be abused as an unauthenticated force-logout/DoS. Tests in store/auth/server cover each property.
- [x] (1) Confirm CSRF protection on every state-changing endpoint (or that the auth model makes it moot) — done 2026-06-24: the CSRF gate lives in `requireAuth` (cookie + unsafe method needs same-origin Origin/Referer or `X-Indiepg-Csrf`, else 409 CodeSafety before the handler). Added `TestEveryStateChangingEndpointRejectsCSRF`, which `chi.Walk`s the real route table and asserts every unsafe-method route rejects a forged cross-origin cookie request — except a documented exempt set (login: needs password; logout: gates rotation via `logoutAuthorized`). Self-checks the exempt list against registered routes, so a new mutating route outside `requireAuth` fails the test. Reviewed (no blocking issues).
- [x] (1) Verify the read-only role is enforced at the DB level (not just UI): the read-only pool/role cannot write even if the API is bypassed — done 2026-06-24: added `TestReadOnlyRole_DBLevelWriteDenial` (integration-tagged, skips without `INDIEPG_TEST_SOCKET`). It connects the real Manager, has the admin pool create+seed a table granting the read-only role SELECT only, then proves: (a) a write via `ExecuteRead` (the unclassified query-box path) is refused with the read-only-transaction default in place — defense in depth; (b) after the role flips its own `default_transaction_read_only` GUC OFF on a real read-pool connection, every write against operator data (INSERT/UPDATE/DELETE/DROP) is STILL refused with `42501` insufficient_privilege — proving privilege denial, not the GUC, is the authoritative boundary. Proven green against a throwaway PG14 cluster, and verified non-vacuous (granting the role write makes it fail at the 42501 assertion). Reviewed.
- [x] (1) Close the read-only CREATE-via-PUBLIC residual — done 2026-06-24: `provisionSQL` now `REVOKE CREATE ON SCHEMA public FROM PUBLIC` + `GRANT CREATE ON SCHEMA public TO indiepg_admin`, scoped to the panel-managed `postgres` DB only (provisionSQL only ever runs against `postgres`; operator app DBs untouched, an accepted app-DB-only limitation that never touches operator data). Extended `TestReadOnlyRole_DBLevelWriteDenial` to assert `CREATE TABLE` by the read-only role is denied with `42501` even with the GUC off, and that admin CREATE still works in the `postgres` DB. Proven green against a real PG14 cluster and non-vacuous (old SQL → read-only CREATE succeeds). Also fixed an in-passing regression the reviewer caught: the `ALTER ROLE` (re-provision) branch dropped `NOINHERIT`, silently leaving the read-only role `INHERIT` on any second provision — restored it (verified `rolinherit=f` after a double-provision) since it is load-bearing for the same privilege-denial boundary.
- [x] (1) Secrets at rest: S3 creds, Pushover tokens, admin hash in the state DB are never logged and the DB file is `0600` — done 2026-06-24: the DB file `0600` + dir `0700` hardening (`ensureSecureStateFile`) and its mode test already existed; this closed the log-scrubbing half. Inventoried every secret-bearing struct — `config.S3Target` (SecretKey, CipherPass), `store.AuthRecord` (Argon2id PasswordHash + raw HMAC SessionSecret), `server.alertChannelConfig` (PushoverToken, WebhookURL) — and made each implement `fmt.Stringer` + `slog.LogValuer` + `fmt.GoStringer`, rendering secrets via new `core.Redact`/`core.RedactBytes` so no log line, error string, or fmt verb (`%v/%+v/%s/%#v`, incl. nested-in-parent `%+v`) can leak them. Non-secret ids kept visible to match the codebase's own boundary (S3 AccessKey is `json:"access_key"`; PushoverUser is kept by `maskAlertChannels`). Log-scrubbing tests per struct assert secrets absent + redaction marker present across text+JSON slog handlers and all fmt verbs. JSON API output unaffected (`json:"-"` + LogValuer doesn't touch encoding/json). Reviewed; closed the `%#v` GoStringer gap the reviewer flagged.
- [x] (1) Login brute-force protection: rate-limit / lockout after N failed attempts with backoff — done 2026-06-24: the lockout (LockoutPolicy default 5 failures/15min window → 15min lock, sliding window, unlock-after-LockFor, success resets) was already built + unit-tested in `internal/auth`. Closed the real gap: proved it end-to-end through the HTTP login handler with `TestLoginLockoutThrottlesAfterMaxAttempts` — N-1 wrong → 401 CodeAuth, Nth wrong → 429 CodeLocked, then the *correct* password is also 429 (lockout checked before password verify, no bypass), and `GET /api/auth/status` surfaces `locked: true` + a future `locked_until`. Flake-proof time assertion. Reviewed. **Band 1 (security) complete.**

## 1.5 · Data durability
- [x] (1.5) Surface "last good backup was N ago" prominently (Dashboard + Backups) — done 2026-06-24: Dashboard already had the "Latest backup" card (result/type/when/age) and the backend field `store.LatestSuccessfulBackup` was already covered by `TestBackupHistory`. Closed the Backups-page gap: added exported `backupFreshness()` helper (classifies newest-first history into none/good/stale/never-good) + a prominent `BackupStatusSummary` banner under the page header that shouts (danger tone) when data is unprotected — incl. the case where the MOST RECENT backup failed but an older one succeeded — and shows "Last good backup N ago (type)" otherwise. vitest/RTL test (`Backups.test.tsx`); stale/fail cases pinned to the real server result string `"fail"`. Reviewed.
- [x] (1.5) Loud alert when a scheduled backup fails or hasn't succeeded within its window — done 2026-06-24: added the immediate `backup-failed` default rule (critical, For:0, fires the instant the most recent attempt fails — you can lose data long before the 26h `backup-stale` warning trips) backed by a new `backup.last_failed` snapshot metric, and made BOTH backup metrics real by deriving them from the store in `Collector.enrichBackup` (the host/PG sampler never read the backup tables, so `backup.last_age_seconds` was silently always 0 → `backup-stale` could never fire). Engine test proves `backup-failed` fires on a failed snapshot and recovers on success; collector tests cover failed-latest/success-latest/no-backups. NOTE the firing→notification only reaches the operator once the evaluation LOOP below is wired (currently dormant).
- [x] (1.5) **Wire the dormant telemetry sampling + alert evaluation loop into the server runtime** — done 2026-06-24: `ListenAndServe` now starts a background loop (`internal/server/background.go`): `startBackgroundJobs` seeds `alert.DefaultRules()` idempotently (never clobbers an operator's edit/disable — only inserts missing IDs), creates a `scheduler.Scheduler`, and registers a `telemetry-sample` cron job on `cfg.Schedules.TelemetrySample` (default `@every 30s`). Each tick `runTelemetryCycle` calls `Collector.SampleOnce` (host/PG + backup health, and buffers samples for the dashboard) → `Engine.Evaluate` → `dispatchAlertEvents` to every enabled stored Pushover/webhook channel via `activeNotifiers`. Loop is tied to ctx (stops on shutdown; deferred Stop is deterministic; in-flight notifier requests are ctx-bound so Stop returns promptly). Collector+Engine built in newServer; OTLP exporter left nil (degrades gracefully — see new backlog item). Tests: seed idempotency + preserves a disabled rule; full cycle fires backup-failed and delivers a "firing" payload to an httptest webhook (+ state persisted); fires-with-no-channel is a clean no-op. Reviewed (feature-dev:code-reviewer): bounded the startup seed with a 30s timeout, added a default-branch warn for unknown channel kinds, documented the ctx-cancellation shutdown behavior.
- [x] (1.5) **Wire scheduled backups (full/incremental) into the runtime** — done 2026-06-24: `startBackgroundJobs` now registers `full-backup` + `incremental-backup` cron jobs from `cfg.Schedules` (defaults: weekly full Sun 03:00, daily incremental Mon–Sat 03:00). Each job (`scheduledBackup(type)`) self-heals the pgBackRest config (`config.Load`→`ensureBackupConfigured`, the same prereq the manual button does — a panel that started before PG was reachable never wrote it) then calls `backups.Backup`. Empty spec = operator opt-out → job not registered + a loud Warn. A left-alone box now backs itself up; before this, the scheduler was instantiated but only ran the telemetry loop, so **scheduled backups had never run** — backups only happened via `POST /backups/run`. CLOSED THE OVERLAP GAP the note warned about: the Owner guard is cross-PANEL only (repo markers), it does NOT stop two concurrent `Backup()` calls in the same process — they'd both collide on pgBackRest's own on-disk lock and the loser would be recorded as a `fail` row → false critical `backup-failed` alert. Added a process-local single-flight guard (`Manager.backupMu` via `TryLock`): an overlap (manual during scheduled, or a long full overlapping the next incremental) returns a typed `CodeConflict` SKIP with NO history row, and the scheduled job swallows `CodeConflict` (logs Info, returns nil) so the scheduler never logs it as a failure. No reentrancy: `Restore`→`takeSafetyBackup`→`Backup` does not hold `backupMu`, so the nested safety backup acquires it cleanly (and a restore launched during an active backup safely HARD-STOPs — it can't take its pre-restore safety backup). Tests: manager concurrent-skip (no fail row, pgBackRest never invoked); server jobs-registered + empty-schedule-disabled. Reviewed (feature-dev:code-reviewer): no blocking issues. NOTE RestoreTest/Digest are NOT wired here: restore-test EXECUTION is still a stub (`handleRestoreTest` returns a typed "not implemented"), and there is no digest builder — both are their own items below, not load-bearing for "never lose data".
- [x] (1.5) Nudge backups off-host: make S3 the recommended default and warn clearly when only local backups exist — done 2026-06-24: the nudge already shipped (Backups badge + warn Callout linking to Settings; Settings "recommended for off-server protection" copy + local-only warning) but the local-vs-S3 logic was inline and untested. Closed the "covered by a test" gap: extracted exported pure `backupDestination(backup, loaded)` (`loading|local|s3` union) + `LocalBackupWarning` component from `Backups.tsx`, behavior unchanged, with 9 vitest/RTL tests (classifier across all cases incl. whitespace-only→local; warning fires only when local). Documented the deliberate `.trim()` divergence from the server's untrimmed `remoteTargetConfigured`. Reviewed (no blocking issues).
- [x] (1.5) Restore-test surfacing: show when the last restore-test ran and whether it passed — done 2026-06-24: the backend field (`store.RestoreTestRecord` + `ListRestoreTests`, mapped to the `restore_tests` wire payload) and the history table already existed; closed the at-a-glance + test gap. Added exported pure `restoreTestStatus(tests)` (`never|passed|failed|never-passed` union, mirroring `backupFreshness` and reusing the shared SUCCESS/FAILURE vocab) + a `RestoreTestStatus` banner above the Restore-tests card answering "have my backups been proven recoverable, and when?" in one line. The "never" state is deliberately CALM (info tone, no call-to-action) because execution isn't wired yet — it must not nudge toward the still-stubbed button; failed/never-passed shout danger for when tests actually run. 9 vitest/RTL tests (classifier × all branches incl. unknown-latest; component × all tones incl. verified-rows display). Reviewed (feature-dev:code-reviewer): no blocking issues.
- [x] (1.5) Restore-test EXECUTION: make "Test a restore" actually verify a backup is recoverable — done 2026-06-24: chose the DESIGN-FIRST option (a) `pgbackrest verify` (repo checksum/WAL integrity, read-only, no restore, no disk cost). Rationale = the security/safety tie-break: verify never touches the live data dir, needs NO disk-headroom precheck or scratch cleanup, and cannot itself cause data loss — the smallest slice that removes the most risk. `Manager.RestoreTest` runs `VerifyCmd`, verifies (not claims) ownership like Restore's read side (foreign owner = HARD STOP), records a pass/fail `restore_tests` row on a detached ctx (survives shutdown), and labels the verified backup. `handleRestoreTest` wired to it (was a stub returning "not implemented"). Unit-tested via the fake Runner (asserts it NEVER invokes backup/restore, records success+fail, persists fail row on cancelled ctx, foreign-owner hard-stop, invalid-stanza). Tightened the UI to be honest: success banner now "Your backup repository is verified intact" (was the overclaiming "proven recoverable") + Callout copy describes the checksum check, not a full restore. Reviewer-flagged copy overclaim addressed. NOTE the heavier scratch-restore-and-boot proof (which would populate `verified_rows` with a real row count) is left as the follow-up below — verify is strong evidence but not a full recovery drill.
- [x] (1.5) Wire the restore-test job into the runtime scheduler — done 2026-06-24: the verify-based `Manager.RestoreTest` only ran on a manual "Test a restore" click; the `restore-test` cron job was never registered in `startBackgroundJobs` (only telemetry + full/incremental backups were), though `cfg.Schedules.RestoreTest` (default `0 5 * * 0`) already existed. Registered it so a left-alone box proves its backups recoverable on a cadence — the "proven recoverable?" banner no longer sits at "never" forever while the repo silently rots. Extracted a generic `registerJob` helper (DRY with `registerBackupJob`). SAFETY: `scheduledRestoreTest` deliberately skips `ensureBackupConfigured` (its `stanza-create` exclusive lock + possible PG restart would collide with a 03:00 full backup still running at 05:00) — verify is a pure reader needing only the already-written config. Reviewer (feature-dev:code-reviewer) caught that collision; fixed before commit. Tests: registered-alongside-backups + empty-schedule-disables. NOTE the digest job is still unregistered (no digest builder exists yet — its own future item, not load-bearing for "never lose data").
- [x] (1.5) Restore-test DEEP proof (option c) — CORE done 2026-06-24: `Manager.RestoreTestDeep` (internal/backup/restore_deep.go) performs a real, non-destructive restore into a fresh `os.MkdirTemp` scratch dir (`--pg1-path` override, never the live data dir), boots it with `pg_ctl` (full WAL replay) on a PRIVATE unix socket (`listen_addresses=` empty, `unix_socket_directories=<scratch>`, `archive_mode=off` both in the restore config AND the boot opts), runs an estimated user-table row count, records `verified_rows` into a `restore_tests` row, then ALWAYS tears down (deferred stop + `os.RemoveAll`, detached ctx). Guards: foreign-owner HARD STOP (read-side ownership), no-backup → NotFound, and a disk-headroom precheck (`free >= dbSize × 1.25` via injectable `diskFree`/statfs; `os.MkdirAll` the scratch root first so the statfs is reliable) that refuses with CodeSafety and issues NO restore when the volume is tight — so a restore can never fill the box and threaten the live dir. Postgres binaries resolved from the restored cluster's PG_VERSION (`/usr/lib/postgresql/<major>/bin`, validated chars). Wired behind an explicit opt-in: `POST /backups/restore-test?deep=true` (default stays the cheap `verify`). Unit-tested via fake Runner: happy path asserts the EXACT scratch dir is the restore target + boot socket (not a prefix coincidence) and never runs `backup`; isolated boot-failure records a fail row while cleanup-stop still runs; headroom-refusal issues no restore + no row; foreign-owner/no-backup/invalid-stanza; real OS seams `defaultDiskFree`/`defaultResolvePGBin` tested directly. Reviewer (feature-dev:code-reviewer): fixed 2 vacuous test assertions (start/stop now matched on distinct tokens; socket isolation compared to exact scratch dir), quoted the socket dir for space-safety, and MkdirAll the scratch root. REMAINING (own items below): a real end-to-end integration test (needs a populated pgBackRest repo) and the UI opt-in button.
- [x] (1.5) Restore-test DEEP — end-to-end integration test — done 2026-06-24: committed `internal/backup/restore_deep_integration_test.go` (`//go:build integration`, env-gated on `INDIEPG_PG_BINDIR` + `pgbackrest` on PATH; never runs in the untagged loop gate). Stands up a throwaway PG cluster + local pgBackRest stanza, takes a real full backup of 1234 seeded+ANALYZEd rows via `Manager.Backup`, then asserts `RestoreTestDeep` restores into a scratch dir + boots (full WAL replay, private socket) + records a `success` row with `verified_rows > 0` + tears the scratch dir down. `stripUserRunner` drops production `AsUser="postgres"` + injects `PGBACKREST_CONFIG` so every binary runs as the current user (no sudo); `resolvePGBin` pinned to the test bin dir. Compiles under `-tags integration` (vet clean), normal gate untouched. Reviewed (feature-dev:code-reviewer): rejected the one "critical" port-5499-collision finding as factually wrong (both clusters use unix sockets in distinct dirs with empty `listen_addresses` — restore_deep.go designs against exactly this). **Last load-bearing band-1.5 item → band 1.5 (data durability) COMPLETE; next is band 2 (stability).**
- [x] (1.5) Restore-test DEEP — UI opt-in button — done 2026-06-24: added a clearly-labeled "Deep restore test" button on the Backups page next to "Test a restore", gated behind `DeepRestoreTestConfirm` (exported, tested) whose copy states up front what it does + both costs (runs longer, needs free disk ≈ DB size) + safety (live DB never touched, scratch copy deleted, refuses rather than fill disk) before it runs. API client `runRestoreTest({ deep })` appends `?deep=true`; default "Test a restore" stays the cheap read-only verify; the `verified_rows > 0` result branch already renders the outcome. vitest/RTL: closed-renders-nothing, copy asserts each material claim, confirm/cancel callbacks, busy disables. Full web + Go gates green. Reviewed (no blocking issues).

## 2 · Stability
- [x] (2) Audit every web API call for explicit error + loading + empty states (no silent failures, no infinite spinners) — done 2026-06-24: audited every data-fetch surface — `useAsync`/`usePolling` consumers (Dashboard, RolesDatabases, Backups, Alerts, Settings, Migrate) and the local-state forms all already had loading + error + empty paths. Found + closed the ONE real silent-failure gap: `usePolling` keeps the last good `data` and only sets `error` on a failed poll, but the three pollers gated their error UI on `!data`, so a poll that started FAILING after the first successful load was silently swallowed (Dashboard froze on a stale "● Healthy" badge while unreachable; the two Migrate progress views kept a spinner/stepper that looked like live progress). Added a reusable `StaleBanner` (warn, `role="alert"`, keeps cached data visible + says the latest refresh failed with the message/hint) surfaced whenever `error` accompanies cached data — Dashboard always, the Migrate pollers (`DirectJobProgress`, `SessionProgress`) only while `!terminal` so a post-completion blip doesn't alarm. Tests: `StaleBanner` unit tests + a Dashboard view test (mocks `usePolling`: banner on error+data, hidden on clean poll, full `ErrorNotice` still on first-load failure); non-vacuous. Reviewed (feature-dev:code-reviewer) — flagged `SessionProgress` as a missed third poller, fixed before commit. **Band 2 (stability) complete → next is band 2.5 (resource & config safety).**
- [x] (2) Provisioning is idempotent: re-running setup on an already-provisioned box is safe and reports "already done" — done 2026-06-24: the flow was already idempotent (apt install / `systemctl enable --now` no-op when done; `provisionSQL` guards every stmt with DO/IF NOT EXISTS), but nothing proved a re-run is a no-op and `Provision` always reported "Postgres provisioned" regardless. Surfaced an HONEST "already done" signal for the one genuinely state-mutating step (`EnsureSocketAuth` → pg_hba.conf, which already returns a `changed` bool): `Provision` result data now carries `socket_auth: "configured"|"already-present"` and install.go logs it. Scoped the claim to that step only — the message does NOT assert the whole provision was a no-op (apt could have upgraded a package). Added `TestProvision_SecondRunIsIdempotentNoOp` (two `Provision` calls share one FakeRunner + one persistent real pg_hba.conf): asserts the second run succeeds, the managed block appears EXACTLY ONCE (a duplicate would corrupt auth), no extra `pg_reload_conf` fires, and `socket_auth` flips to `already-present`. Proven non-vacuous (breaking `injectHBARules` idempotency fails it). Reviewed (feature-dev:code-reviewer): no blocking issues.
- [x] (2) Verify query-box guards: auto-LIMIT and statement_timeout actually applied to the read pool — done 2026-06-24: auto-LIMIT was already proven (guard `Check`/`injectLimit`/`EnsureLimit` unit-tested in `internal/pg/guard`, server wires `guard.New({ReadOnly:true, AutoLimit: cfg.QueryLimit})`, handler runs `s.guard.Check` before `ExecuteRead`). Closed the statement_timeout gap: it was only proven at the DSN-string level (buildDSN test asserts `statement_timeout=30000` present) — nothing proved Postgres enforces it at runtime on the read pool. Added `TestReadOnlyPool_StatementTimeoutEnforced` (`internal/pg/readonly_integration_test.go`, `//go:build integration`, env-gated on `INDIEPG_TEST_SOCKET`; never runs in the untagged loop gate): 250ms read-pool cap + 30s client ctx so the cancel is the SERVER not the Go deadline → (1) `ExecuteRead("SELECT pg_sleep(5)")` cancelled with "statement timeout" in <5s (the real query-box path), (2) raw read-pool conn proves wire-level SQLSTATE `57014` (query_canceled), (3) positive control: `pg_sleep(0.5)` on the priv pool COMPLETES (cap scoped to read pool only). Proven green against a throwaway PG14 cluster (both integration tests pass; ~1.0s runtime, not 5s+, confirms the cap fired). Reviewed: no blocking findings.

## 2.5 · Resource & config safety
- [x] (2.5) Disk headroom alert: warn well before the data/WAL volume fills (default threshold) — done 2026-06-24: the panel already shipped a CRITICAL `disk-almost-full` (90% on `host.disk_percent`, a statfs of the PG data/WAL volume), but 90% is the emergency, not an early warning. Added an early-warning tier `disk-headroom-low` (WARNING, `>= 80%`, `For:5m`, `Cooldown:1h`) to `alert.DefaultRules()`, mirroring the backup-stale+backup-failed two-tier pattern — gives the operator runway (prune WAL / grow the volume / archive) before a slow fill can stop Postgres. Rule-only: `MetricDiskPercent` and the seed/evaluate loop already exist; `seedDefaultAlertRules` inserts only missing IDs so an upgrading panel auto-seeds it without clobbering operator edits. The 5m For window ignores a transient bump (e.g. a deep restore-test scratch copy on the same volume); above 90% both tiers are independently active (intended escalation). Tests: updated `TestDefaultRules` want-list + new `TestDiskHeadroomEarlyWarningTier` (tier ordering: warning lower-severity AND lower-threshold than critical; non-vacuous firing: at 85% the warning fires after its For window while critical stays OK). Reviewed (no blocking issues).
- [x] (2.5) Self-healing config: applying a postgresql.conf change (default or override) that prevents Postgres from starting auto-rolls-back to last-known-good and surfaces the error; PG never left down — done 2026-06-24: built the core primitive in `internal/pg/safeconfig.go` — `snapshotAutoConf` captures `<data_directory>/postgresql.auto.conf` BEFORE a change (fails closed if unreadable: a restart we can't undo must never proceed); `restartWithRollback` restarts Postgres and, on a non-zero `systemctl restart` exit (authoritative "didn't come back up" for the synchronous postgresql unit), restores the snapshot via `tee` as the postgres user (keeps owner+0600; file restore works even while PG is down, unlike `ALTER SYSTEM RESET`) and restarts again — PG never left down — returning a typed `CodeSafety` ("rolled back; Postgres is running") or, if the rollback restart also fails, `CodeInternal` ("Postgres is down") with a manual-fix hint. Made it load-bearing now (not dead code): routed `EnsureArchiving`'s restart-requiring path through it (snapshot before its ALTER SYSTEM loop) and fixed `ensureBackupConfigured` to stop re-wrapping the typed error as generic `CodeInternal`. Acceptance test simulates a bad setting → exact-content rollback → PG up → CodeSafety; plus rollback-restart-also-fails, no-rollback-on-success, snapshot-fails-closed, and the snapshot-before-write ordering invariant. Go gate green; web untouched. Reviewed (all 3 findings addressed). NOTE: no operator override-apply UI exists yet — the two band-2.5 items below (host tuning, conn-saturation alert) and any future override flow must funnel their restart through this primitive.
- [x] (2.5) Host-sized tuning at provision time using DEFAULTS.md math (shared_buffers/work_mem/effective_cache_size/max_connections from detected RAM/CPU; workload profile selectable, default mixed) — done 2026-06-24: added pure `RecommendTuning(memoryMB, cpuCount, profile)` in `internal/pg/tuning.go` faithfully porting the `sm` tuning.py math for the five core settings (shared_buffers/effective_cache_size/work_mem/maintenance_work_mem/max_connections), with selectable `WorkloadProfile` (empty → Mixed default; unknown → ValidationError, no silent mis-size). Total/panic-free on degenerate input. Wired into `Provision` to SURFACE the host-sized recommendation in the result (`recommended_tuning` Data + step note) — informational only, explicitly "not yet applied" (computing it never touches PG, can't fail provisioning). APPLYING (restart-requiring shared_buffers/max_connections) is the separate next item below and MUST funnel through `restartWithRollback`. Acceptance: table test vs sample RAM values (1GB–128GB × oltp/olap/mixed) with hand-computed expecteds, plus clamp/ordering/degenerate/parse/SettingsMap tests. Reviewed (no blocking findings). **Last open band-2.5 item — see the new APPLY follow-up below.**
- [x] (2.5) APPLY host-sized tuning at provision — CORE done 2026-06-24: added `Manager.ApplyTuning(ctx, rec)` (internal/pg/tuning_apply.go) — persists the `RecommendTuning` settings via ALTER SYSTEM and activates them safely. Idempotent: reads current values from `pg_settings` normalised to bytes via the unit column (`settingUnitBytes` handles 8kB/kB/MB/GB and unit-less), skips settings already at the wanted value (so a re-provision is a no-op, no needless restart). Restart-requiring settings (shared_buffers, max_connections) funnel through the iter-26 `restartWithRollback` primitive — snapshots postgresql.auto.conf BEFORE writing, and on a failed restart rolls back to last-known-good (PG never left down) → CodeSafety. Reloadable-only changes use pg_reload_conf with no restart. Integer GUCs (max_connections) written UNquoted, memory settings quoted (reviewer-caught). Wired into `Provision` (replaces the iter-28 "surfaced, not applied"): applies the Mixed-profile host-sized defaults, and GRACEFULLY tolerates a CodeSafety rollback (logs a warn, records `tuning: "rejected (rolled back...)"`, carries on — an oversized default must not fail the whole provision). Result data carries `tuning` status + keeps `recommended_tuning`. Added a `detectTuning` test seam on Manager so provision tests pin RAM/CPU deterministically. Tests: ApplyTuning no-op/reload-only/restart/restart+reloadable/rollback-on-restart-fail/missing-setting/no-runner + settingUnitBytes table + alterValue quoting; provision happy-path + idempotent tests updated to pin tuning (native 8kB/kB units) so they stay deterministic + assert the no-op path. Reviewed (feature-dev:code-reviewer): fixed the integer-GUC quoting and the synthetic-MB test-unit gap. REMAINING split into the two items below (real-PG integration proof + UI) — neither load-bearing for the apply logic itself, which is unit-proven.
- [x] (2.5) APPLY host-sized tuning — real-PG integration test — done 2026-06-24: `internal/pg/tuning_apply_integration_test.go` (`//go:build integration`, env-gated on `INDIEPG_PG_BINDIR`, never in the untagged loop gate). Stands up a throwaway PG14 cluster on a private socket and asserts (1) a sane recommendation lands in `pg_settings` (memory normalised to bytes via the unit column, max_connections exact) — proving the restart-requiring settings took effect via a real restart; (2) a second apply of the same rec is a no-op (changed=false, no restart); (3) self-healing — a restart-requiring value the postmaster refuses to boot with rolls back via `restartWithRollback` with PG still UP and a `CodeSafety` error. A `tuningTestRunner` wraps the real OSRunner (strips AsUser, injects PGHOST/PGPORT/PGUSER, translates the exact `systemctl restart postgresql` → `pg_ctl restart` of the test cluster) so the real ApplyTuning path runs unmodified. Used `max_connections=1` (cross-GUC boot failure) NOT an oversized shared_buffers for the rollback — the latter HANGS under Linux memory overcommit; and `pg_ctl -l <logfile>` is required or the daemonized postmaster holds the runner's stdout pipe open and `cmd.Run` blocks forever. Proven green against real PG14 (~1.5s). Reviewed (feature-dev:code-reviewer): tightened the systemctl intercept to the exact 2-arg invocation + documented the as-current-user cat/tee divergence.
- [x] (2.5) APPLY host-sized tuning — UI surface — done 2026-06-24: provisioning is CLI-driven, so the `recommended_tuning`/`tuning` fields were only logged. Added a READ-ONLY surface — `Manager.CurrentTuning` + `GET /api/tuning` returning detected RAM/CPU, live applied settings (from `pg_settings`, whole MB; `Applied=nil` best-effort when PG unreachable), and the host-sized recommendation per profile — and a "Database tuning (host-sized)" card on Settings showing the applied settings (each plainly described) plus a profile selector that PREVIEWS each profile's sizing labeled by effect. A non-default selection clearly states it's a preview that changes nothing and that a real switch needs a brief Postgres restart (so it's a provision-time action, not a panel button — security tie-break: no restart-trigger surface). Tests: `TestCurrentTuning_*` + `DatabaseTuning.test.tsx`. Reviewed (no blocking issues). **Band 2.5 (resource & config safety) COMPLETE → next is band 3 (usability).**
- [x] (2.5) Connection saturation alert: warn as active connections approach max_connections — done 2026-06-24: the WARNING tier (`connections-near-max`, `pg.connections_percent` >= 85%, For 2m) already shipped and was present-tested, BUT it was the only tier — at ~max_connections Postgres refuses new clients (an outage; apps can't connect, and the panel itself can be locked out once only `superuser_reserved_connections` remain), with no louder page before that. Added a CRITICAL escalation tier `connections-critical` (>= 95%, For 1m, Cooldown 15m, Severity Critical) mirroring the disk-headroom-low → disk-almost-full two-tier pattern: higher threshold but shorter For + the 15m cooldown of the other outage-class rules (pg-down), with 95% leaving a sliver of runway to kill runaway sessions. Metric/sampling were already real (sampler.go counts pg_stat_activity / current_setting('max_connections') → snapshot → metricValue), seeding is ID-based so it auto-adds on upgrade without clobbering edits. Updated TestDefaultRules want-list + added TestConnectionSaturationTiers proving tier ordering AND non-vacuous firing of BOTH tiers (warning fires at 90% while critical stays OK; critical fires at 97% past its 1m For). Reviewed (feature-dev:code-reviewer): no blocking findings.

## 3 · Usability
- ~~(3) Provision flow shows the computed best-defaults up front with each override clearly labeled by effect~~ — DROPPED 2026-06-24: provisioning is CLI-driven (`indiepg install`); there is NO provision flow in the panel (App.tsx has no setup route), so this item has no surface. The intent is already served read-only by the "Database tuning (host-sized)" card on Settings (iter 31), which shows the computed host-sized defaults per profile, each labeled by effect.
- [x] (3) Every destructive action's confirm dialog states exactly what will happen and what is irreversible — done 2026-06-24: audited all confirm sites (Backups run/deep-test/restore, Alerts delete-rule, RolesDatabases drop db/user, Migrate single/cluster overwrite) — each already states the consequence + irreversibility, and destructive ops gate behind a typed exact-name match. The audit was UNTESTED, so I added `web/src/components/ConfirmDialog.test.tsx` (8 tests) locking the two safety invariants: plain-language consequence is shown, and `TypedConfirmDialog` cannot fire `onConfirm` until the exact object name is typed (also gated while busy). Reviewed (feature-dev:code-reviewer: 2 robustness findings on assertion fragility, both applied).
- [ ] (3) PgBouncer opt-in pooler — being built incrementally (mirroring how RecommendTuning was: pure math → render → apply → UI). Sub-items:
  - [x] (3) Pure host-sized pool-sizing math — done 2026-06-24: `internal/pgbouncer/pool.go` `RecommendPool(pgMaxConnections, profile)` faithfully ports the `sm` pool math (DEFAULTS.md): available = max_conn − 5 reserved, default_pool_size = int(available × util) floored at 20 (util oltp .80/mixed .70/olap .60), min = default/4 floor 5, reserve = default/5 floor 5, max_client_conn = default × multiplex (20/10/5), server_idle_timeout 300/600. Reuses `pg.WorkloadProfile` (unknown → Mixed; max_conn < 1 clamped) so it's total/panic-free. Fixed safe defaults are hardcoded constants (auth_type scram-sha-256, pool_mode transaction, server_reset_query DISCARD ALL — never weakened), exposed via `SettingsMap()` for a future read-only preview. Table + invariant (incl. floor-activation at small boxes) + degenerate/unknown + SettingsMap tests. Reviewed (feature-dev:code-reviewer): no critical; applied both important findings (corrected DEFAULTS.md `round`→`int` truncation to match `sm`; widened invariant sweep to self-cover the floors).
  - [x] (3) pgbouncer.ini config rendering from a PoolRecommendation — done 2026-06-24: `internal/pgbouncer/config.go` `RenderConfig(ConfigParams)` renders the full pgbouncer.ini from a `PoolRecommendation` + host bits, all fields safe-defaulted. Pinned loopback-only (`listen_addr`/`[databases]` host both 127.0.0.1 — non-configurable, never widened), `[databases] * = host=127.0.0.1 port=5432`, hardened constants (scram/transaction/DISCARD ALL) from pool.go. Deterministic (fixed line order) so an unchanged config can skip a rewrite/reload. Validates every interpolated value against config injection (control chars, U+2028/29 line separators, and `#`/`;` comment starters that would silently truncate a value), ports + collision rejected, and refuses a zero/negative pool (would disable limits/idle-timeout). `ConfigMarker` ownership guard (first-line, mirrors pgBackRest) + `HasManagedMarker` for the installer slice. Pure render — no install/file-write yet (atomic write owner `pgbouncer` 0640 lands in the install slice). Reviewed (feature-dev:code-reviewer): applied the critical (validate Pool numbers) + all 3 important findings (reject `#`/`;`, add `::` to the never-widen test, zero-pool rejection test).
  - [x] (3) Config installer (atomic write) — done 2026-06-24: `internal/pgbouncer/install.go` `Manager.EnsureConfig(ConfigParams) (changed bool, err)` renders the pgbouncer.ini and installs it atomically (temp+rename), owner `pgbouncer` 0640 (DEFAULTS.md; no secret in the .ini), marker-guarded so a hand-written/distro pgbouncer.ini is surfaced as `CodeConflict` not clobbered, deterministic no-op when unchanged (so the enable flow can skip a needless reload). O_NOFOLLOW path-hijack guard refuses a symlink at the config path as a clear conflict (not opaque internal error). Mirrors the reviewed pgBackRest installer (readNoFollow / writeConfigFile / chownToBouncerUser, root-fatal/non-root-best-effort chown). No package-install/service-touch yet (that's the enable slice). Tests: writes-marked-0640, idempotent-no-rewrite (mtime-stable), rewrites-on-change, refuses-foreign + marker-not-first-line, refuses-symlink, rejects-injection-before-write, requires-runner, default-confdir. Reviewed (feature-dev:code-reviewer): applied the symlink-clarity fix.
  - [x] (3) SCRAM userlist render — done 2026-06-24: `internal/pgbouncer/userlist.go` `RenderUserlist([]UserlistEntry) (string, error)` builds the auth_file (userlist.txt) text — `"username" "verifier"` lines, the `sm` format. Pure render, no file I/O (the atomic install lands in the install/enable slice). Security-strict: at least one entry required (an empty auth_file would lock every app out); EVERY verifier must be `SCRAM-SHA-256$…` and drawn only from the verifier's own alphabet (`[A-Za-z0-9+/=:$-]`) — md5/plaintext refused, never downgraded, and the charset doubles as the injection guard for the quoted token; usernames with a quote/whitespace/control/line-separator are rejected (not escaped) so a second auth entry can't be injected; duplicate usernames refused (pgbouncer silently honours only the first). Deterministic (sorted by username) so an unchanged user set renders byte-identical → enable flow can skip a needless reload. Mirrors the pure-render pattern of config.go/pool.go. Tests: line-format, sorted-stable/order-independent, empty-refused, non-SCRAM-refused (md5/plaintext/wrong-case/wrong-algo), verifier-injection (quote/newline/space/`#`), username-injection (quote/space/tab/newline/U+2028/U+2029/NUL/empty), duplicate-refused, no-write-on-error. Reviewed (feature-dev:code-reviewer): two test-hardening findings applied (explicit U+2028/U+2029 cases; CodeValidation assertion on the empty-slice path).
  - [x] (3) Userlist auth_file atomic installer — done 2026-06-24: `internal/pgbouncer/install.go` `Manager.EnsureUserlist(ctx, []UserlistEntry) (changed bool, err)` renders the auth_file (RenderUserlist) and installs userlist.txt atomically, mirroring `EnsureConfig`'s contract: temp+rename, 0640 owned by the pgbouncer user (auth_file holds secret-adjacent SCRAM verifiers), O_NOFOLLOW symlink-hijack guard → `CodeConflict`, deterministic no-op when content is byte-identical (enable flow can skip a reload). NO foreign-file marker guard (unlike the .ini): userlist.txt is pure `"user" "verifier"` lines and can't carry a marker — confirmed against `sm`; ownership is gated upstream by EnsureConfig's .ini marker guard, the auth_file being a fully indiepg-derived satellite. Logs only the user COUNT, never a verifier. DRYed the shared atomic-install logic out of `writeConfigFile` into package-level `atomicInstall(path, data, mode)` (both .ini and auth_file use it; EnsureConfig behavior unchanged). Tests userlist_install_test.go: writes-0640 (exact RenderUserlist bytes), idempotent-no-rewrite (mtime-stable, two-user set re-submitted in reverse input order proving sort-stable no-op), rewrites-on-change, refuses-symlink (target untouched), rejects-non-SCRAM + rejects-empty before any write, requires-runner, default-path. Reviewed (feature-dev:code-reviewer): no blocking findings; applied the one Important test-fix (idempotency test now actually reorders a two-user set). REMAINING for the enable flow: query pg_authid for the app role(s)' verifiers, package install (apt) + service lifecycle (`systemctl enable --now` + SIGHUP reload/restart-fallback only when changed + verify running).
  - [x] (3) Query pg_authid for the app roles' SCRAM verifiers — done 2026-06-24: `internal/pg/verifiers.go` `Manager.RoleVerifiers(ctx, roleNames []string) ([]RoleVerifier, error)` reads `pg_authid.rolpassword` for the named app roles — the one input `EnsureUserlist`/`RenderUserlist` still needed fed. `rolpassword` is superuser-only so it ALWAYS uses the privileged psql path (`runPsql`, postgres OS user, peer auth), never the non-superuser read pool. Secret-adjacent: logs nothing (runPsql logs only argv, never stdout); query has no PASSWORD literal so Sensitive stays false. Strict (auth_file is a boundary): every name `ValidateIdentifier`-gated AND `QuoteLiteral`'d into the IN-list; missing role → NotFound naming it; NULL/empty rolpassword → Validation error (never silently dropped — a missing entry would lock that app out); duplicates de-duped; deterministic sort. SCRAM-vs-md5 NOT checked here (RenderUserlist is the single gate; verbatim pass-through proven by test). Tests: superuser-path, non-SCRAM pass-through, de-dup, missing-role, empty-verifier, invalid-name-before-any-query, requires-runner, empty-request. Reviewed (feature-dev:code-reviewer): applied the one Important finding (dropped a dead `ORDER BY`).
  - [x] (3) Service-lifecycle primitives — done 2026-06-24: `internal/pgbouncer/service.go` adds four methods on `Manager`, mirroring `pg/manager.go`: `InstallPackage` (`apt-get update` + `install -y pgbouncer`, noninteractive, idempotent; update-failure stops before install), `EnableNow` (`systemctl enable --now`, idempotent), `Reload` (tries `systemctl reload`/SIGHUP first — re-reads config + reopens auth_file without dropping client conns — then falls back to `systemctl restart`; both-fail → CodeExec; the enable flow calls it ONLY when config/auth_file changed), `IsRunning` (`systemctl is-active`: reported non-active state → clean false; empty-stdout + runner error → surfaced as error so the verify-after-start can't mistake "couldn't ask" for "down"). Not yet wired — orchestrator + UI are the slices below. Tests service_test.go (16): ordering/env, reload-prefers-SIGHUP, reload→restart fallback, both-fail, is-active true/false/undeterminable, requires-runner. Reviewed (applied IsRunning error-surfacing #1 + test #2; rejected #3 — `WarnCtx` doesn't exist on `core.Logger`).
  - [x] (3) Enable flow backend ORCHESTRATOR — done 2026-06-24: `internal/pgbouncer/enable.go` `Manager.Enable(ctx, src VerifierSource, state PoolerState, p EnableParams) (EnableResult, error)` wires the six primitives into one opt-in entry point. OFF by default; idempotent/re-runnable. Order: InstallPackage (creates the pgbouncer user + /etc/pgbouncer first) → RoleVerifiers (via `src`, satisfied by `*pg.Manager`) → EnsureConfig → EnsureUserlist → EnableNow → Reload ONLY when config/auth_file changed (so an unchanged pooler is never bounced) → IsRunning verify → persist `enabled=true` LAST (only once confirmed up, so the flag can't contradict a failed bring-up). Added `IsEnabled(ctx, state)` (unset key = default-off) for the UI slice; `PoolerState` is a narrow GetConfig/SetConfig interface `*store.Store` satisfies (no store coupling into the package). SECURITY (reviewer-caught): EnsureConfig runs BEFORE EnsureUserlist — its marker guard is the flow's only ownership check, so the secret-adjacent auth_file is never written into a dir indiepg doesn't own (a foreign distro/operator pgbouncer.ini is a hard stop with no auth_file left behind). Logs role COUNT only, never names/verifiers. Tests via fake VerifierSource + in-memory PoolerState + temp confdir (happy-path ordering/mode/auth_file path, idempotent-no-bounce, reload-only-on-change, verifier-error/foreign-config/non-SCRAM all stop before bring-up, not-running-not-recorded, persist-failure-surfaces, input validation, IsEnabled default-off). Reviewed (feature-dev:code-reviewer): both findings applied. REMAINING: the UI toggle slice below.
  - [ ] (3) UI: a pooler toggle (off by default) that, when on, shows the app connection string via the pooler + the pool settings labeled by effect; confirm states exactly what enabling does. vitest/RTL test.

## 4 · UI redo (shadcn)
- [ ] (4) Scaffold Tailwind + shadcn into the Vite app without regressing existing views (config, base components, one pilot view migrated) — acceptance: build green, pilot view visually intact, test passes.
- [ ] (4) Migrate remaining views to shadcn one per iteration (Login, Dashboard, RolesDatabases, Query, Backups, Migrate, Alerts, Settings) — acceptance per view: same behavior, a test, no new complexity.
