# Build artifacts
/indiepg
/dist/
*.test
*.out
coverage.txt
# e2e: the panel binary is staged into the base image build context at build time
/test/e2e/docker/indiepg

# SQLite panel state
*.db
*.db-shm
*.db-wal

# Go caches / vendoring
/.gocache/
/.gomodcache/
vendor/

# Frontend (no Node on the server, ever)
/web/node_modules/
/web/dist/
# NOTE: internal/server/web/dist/ (the built SPA) IS committed on purpose so a
# clean `go build` yields a working UI with no Node toolchain. Rebuild via `make web`.

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local env
.env
.env.local

# ralph loop runtime artifacts
scripts/ralph/run-logs/
scripts/ralph/HALT.md
scripts/ralph/COMPLETE.md
scripts/ralph-ux/run-logs/
scripts/ralph-ux/HALT.md
scripts/ralph-harden/run-logs/
scripts/ralph-harden/HALT.md
scripts/ralph-harden/COMPLETE.md

# local npm cache used by the UI loop (keeps installs off the read-only ~/.npm)
web/.npm-cache/
web/web/

# Root-level Node / npm artifacts (incidental installs; UI loop runs from web/)
/node_modules/
/.npm-cache/

# AI tooling artifacts (Claude Code / skills / agents) — not project source
/.claude/
/.agents/
/.mcp.json
/skills-lock.json

# Harness-injected home dotfiles & config that surface in the working tree
/.bashrc
/.bash_profile
/.profile
/.zprofile
/.zshrc
/.gitconfig
/.ripgreprc
/.gitmodules
/.idea
/.vscode
